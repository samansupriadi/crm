name: Greeting on variable day 2

on:
    push:
      branches :  ["demo", "dev", "test"]
    pull_request:
      branches: ["demo", "dev", "test", "development"]
      types: ["closed"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4 
        
      - name: Run entrypoint.shx
        run: |
          bash entrypoint.sh OWNER=samansupriadi REPO=crm-ops GITHUB_TOKEN=ghp_Erm74f6Xnsm5qk8hannX88g1QwK9C10NeMw WORKFLOW_FILE_NAME=blank.yaml